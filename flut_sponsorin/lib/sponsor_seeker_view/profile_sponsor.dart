import 'package:flutter/material.dart';

class profile_sponsor extends StatelessWidget {
  const profile_sponsor({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
            child: Text(
              'profile Sponsor',
              style: TextStyle(fontSize: 24, color: Colors.black),
            ),
          ),
    );
  }
}