import 'package:flutter/material.dart';

class list_overall_events extends StatelessWidget {
  const list_overall_events({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          'List Overall Events',
          style: TextStyle(fontSize: 24, color: Colors.black),
        ),
      ),
    );
  }
}
